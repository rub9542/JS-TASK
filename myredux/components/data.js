import React from 'react'
import './todo.css'

function Data(props) {
    // const {title}=props
    return (
        <div className='title'>
            {props.title}
        </div>
    )
}

export default Data
